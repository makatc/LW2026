{
  "name": "@lwbeta/monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:migrate": "pnpm --filter @lwbeta/db migrate:up"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "ts-node": "^10.9.1",
    "turbo": "^1.10.0",
    "typescript": "^5.1.3"
  },
  "packageManager": "pnpm@8.0.0",
  "dependencies": {
    "pg": "^8.11.0"
  }
}